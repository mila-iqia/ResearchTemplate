# Pretrained weights should be loaded at the right location
# on $SCRATCH when on a slurm cluster)
# Datasets should be loaded a the right location as well
# Using a pretrained model
## i.e. fire inference once on a pretrained model, after loading the weights
